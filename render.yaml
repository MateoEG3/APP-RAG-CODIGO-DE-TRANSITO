services:
  - type: web
    name: vialex
    runtime: python
    buildCommand: pip install -r requirements.txt && python src/ingestion.py
    startCommand: gunicorn app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false